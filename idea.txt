设计思路：
1. 首先降应力，
   if 不能降到目标值以下，伺服到目标值，结束
   else 可以降低到目标值以下，两种情况：
        if 在第一个伺服应力循环结束时，密实度大于目标值：
            if 最后一个循环，输出，结束
            else 结束，下一个try值
        else 伺服到目标值，结束

全部是，（降应力-伺服到第一个循环结束）
        if in_inac_region2 = True
            output
            write success.txt
            end
        else:
            if measured_density > aim_density:
                if last_try = True:
                    output
                    write success.txt
                    end
                else:
                    end, to next try
            else:
                continue run until converge
    